{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm --prefix frontend install && npm --prefix frontend run build && mkdir -p backend/public && cp -r frontend/dist/* backend/public/ && npm --prefix backend install && npm --prefix backend run build"
  },
  "deploy": {
    "startCommand": "cd backend && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
